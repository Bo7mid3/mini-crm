import { COLORS } from "@constants/theme";

export const DEFAULT_INPUT = {
  inputField: {
    
  },
  inputContainer: {
    marginBottom: 20,
  },
  inputError: {
    color: COLORS.error,
    marginTop: 5,
  },
};
